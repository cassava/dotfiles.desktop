#!/bin/bash

echo "Load US keyboard layout"
setxkbmap -layout us -variant altgr-intl -option terminate:ctrl_alt_bksp,caps:escape,grp:rctrl_toggle,compose:sclk
if [ -f ~/.Xmodmap ]; then
    echo "Load ~/.Xmodmap"
    xmodmap ~/.Xmodmap
fi
